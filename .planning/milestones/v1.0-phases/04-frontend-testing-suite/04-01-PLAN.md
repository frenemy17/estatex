---
phase: 04-frontend-testing-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/setupTests.js
  - frontend/src/lib/api.test.js
  - frontend/src/lib/utils.test.js
  - frontend/src/components/AdminTokenButton.test.jsx
  - frontend/src/components/ProviderStatus.test.jsx
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "@testing-library/react, @testing-library/jest-dom, and @testing-library/user-event are installed in frontend devDependencies"
    - "setupTests.js configures DOM polyfills for matchMedia and ResizeObserver"
    - "All unit tests for lib/api.js and lib/utils.js pass"
    - "All component tests for AdminTokenButton and ProviderStatus pass"
  artifacts:
    - frontend/src/setupTests.js
    - frontend/src/lib/api.test.js
    - frontend/src/lib/utils.test.js
    - frontend/src/components/AdminTokenButton.test.jsx
    - frontend/src/components/ProviderStatus.test.jsx
  key_links:
    - "setupTests.js bootstraps jest-dom matchers"
    - "api.test.js validates token storage, interceptors, score bands, and provider tones"
    - "AdminTokenButton.test.jsx validates token modal, persistence, and custom event dispatch"
---

<objective>
Bootstrap frontend testing infrastructure and author unit tests for client API, utilities, and core shared components.

Purpose: Fulfills initial slice of `UI-01` by equipping the React frontend with `@testing-library`, DOM matchers, and establishing automated test coverage over the core client logic and shared components.
Output: Configured test runner environment, passing unit tests for `lib/`, and passing component tests for `components/`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-testing-suite/04-RESEARCH.md
@.planning/phases/04-frontend-testing-suite/04-VALIDATION.md
@frontend/package.json
@frontend/src/lib/api.js
@frontend/src/lib/utils.js
@frontend/src/components/AdminTokenButton.jsx
@frontend/src/components/ProviderStatus.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Testing Library Packages and Configure setupTests.js</name>
  <files>frontend/package.json, frontend/src/setupTests.js</files>
  <read_first>frontend/package.json</read_first>
  <action>
    1. Install `@testing-library/react`, `@testing-library/jest-dom`, and `@testing-library/user-event` as devDependencies via npm in `frontend/`.
    2. Create `frontend/src/setupTests.js`:
       - Import `@testing-library/jest-dom`.
       - Polyfill `window.matchMedia` for responsive components and dark mode hooks.
       - Polyfill `ResizeObserver` and `IntersectionObserver`.
  </action>
  <verify>npm --prefix frontend test -- --watchAll=false --passWithNoTests</verify>
  <acceptance_criteria>
    - `@testing-library/react` is in `frontend/package.json` devDependencies.
    - `frontend/src/setupTests.js` exists and loads cleanly in Jest.
  </acceptance_criteria>
  <done>Frontend test runner initialized with DOM matchers and polyfills.</done>
</task>

<task type="auto">
  <name>Task 2: Author Unit Tests for API Client and Utilities</name>
  <files>frontend/src/lib/api.test.js, frontend/src/lib/utils.test.js</files>
  <read_first>frontend/src/lib/api.js, frontend/src/lib/utils.js</read_first>
  <action>
    1. Create `frontend/src/lib/utils.test.js`:
       - Test `cn()` merges Tailwind classes correctly, resolves conflicts, and ignores falsy values.
    2. Create `frontend/src/lib/api.test.js`:
       - Test `getAdminToken()` and `setAdminToken()` reading and writing to `localStorage`.
       - Test `onAdminTokenChange()` handler dispatch on token modification.
       - Test `api` Axios interceptor injecting `X-Admin-Token` header when token is present, and omitting when empty.
       - Test `scoreBand(score)` thresholds (Elite >= 85, Qualified >= 70, Nurture >= 40, Cold < 40).
       - Test `providerTone(p)` returning `MOCK`, `LIVE_OK`, and `LIVE_ERROR`.
       - Test `providerHint(p)` strings for live OK, failures, missing envs, and gate closed.
       - Test `isErrorEvent(e)` classifying error kinds and keywords.
  </action>
  <verify>npm --prefix frontend test -- --watchAll=false src/lib/</verify>
  <acceptance_criteria>
    - All tests in `src/lib/api.test.js` and `src/lib/utils.test.js` pass.
  </acceptance_criteria>
  <done>Client library functions and API interceptors verified.</done>
</task>

<task type="auto">
  <name>Task 3: Author Component Tests for AdminTokenButton and ProviderStatus</name>
  <files>frontend/src/components/AdminTokenButton.test.jsx, frontend/src/components/ProviderStatus.test.jsx</files>
  <read_first>frontend/src/components/AdminTokenButton.jsx, frontend/src/components/ProviderStatus.jsx</read_first>
  <action>
    1. Create `frontend/src/components/AdminTokenButton.test.jsx`:
       - Test rendering button with read-only demo badge when no token is present.
       - Test clicking button opens dialog modal (`input-admin-token`).
       - Test typing token and clicking `btn-save-admin-token` updates localStorage and closes modal.
       - Test clearing token clears localStorage.
    2. Create `frontend/src/components/ProviderStatus.test.jsx`:
       - Mock `api.get("/providers")` returning provider status payload.
       - Test rendering provider chip indicators for Groq, Cal.com, Resend, Twilio, HubSpot.
       - Test rendering live green chip vs yellow mock chip based on response data.
  </action>
  <verify>npm --prefix frontend test -- --watchAll=false src/components/</verify>
  <acceptance_criteria>
    - All tests in `src/components/AdminTokenButton.test.jsx` and `src/components/ProviderStatus.test.jsx` pass.
  </acceptance_criteria>
  <done>Core shared components tested with realistic user interactions and mocked API state.</done>
</task>

</tasks>

<must_haves>
  <truths>
    - Testing library packages installed in frontend
    - Unit tests for lib/api.js and lib/utils.js pass
    - Component tests for AdminTokenButton and ProviderStatus pass
  </truths>
</must_haves>
