---
phase: 04-frontend-testing-suite
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - frontend/src/pages/Dashboard.test.jsx
  - frontend/src/pages/LeadDetail.test.jsx
  - frontend/src/pages/Capture.test.jsx
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Kanban board renders all 7 status columns and isolates leads according to status"
    - "Search and category filters in Dashboard correctly filter the visible lead cards"
    - "KPI summaries (Hot, Booked, In-Flight, Approvals, Conversion Rate) calculate accurately"
    - "LeadDetail handles escalation approve/reject actions"
    - "Capture form submits new leads to /api/lead"
    - "Full frontend test suite passes 100% cleanly"
  artifacts:
    - frontend/src/pages/Dashboard.test.jsx
    - frontend/src/pages/LeadDetail.test.jsx
    - frontend/src/pages/Capture.test.jsx
  key_links:
    - "Dashboard.test.jsx tests the Kanban board pipeline and search/filter behavior"
    - "LeadDetail.test.jsx tests escalation and supervisor triggers"
    - "Capture.test.jsx tests lead intake and navigation"
---

<objective>
Author comprehensive component and integration tests for the Kanban board pipeline, Lead Detail page, and Capture intake flow.

Purpose: Fulfills requirement `UI-01` by delivering end-to-end component testing for EstateX's primary user workflows.
Output: Thoroughly tested React views, verified user interaction flows, and a complete green frontend test suite.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-testing-suite/04-RESEARCH.md
@.planning/phases/04-frontend-testing-suite/04-VALIDATION.md
@.planning/phases/04-frontend-testing-suite/04-01-PLAN.md
@frontend/src/pages/Dashboard.jsx
@frontend/src/pages/LeadDetail.jsx
@frontend/src/pages/Capture.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Comprehensive Kanban Board and Dashboard Tests</name>
  <files>frontend/src/pages/Dashboard.test.jsx</files>
  <read_first>frontend/src/pages/Dashboard.jsx</read_first>
  <action>
    1. Create `frontend/src/pages/Dashboard.test.jsx`:
       - Mock `api.get("/leads")` with a fixture dataset representing diverse lead states (NEW, CALLING, IN_CONVERSATION, QUALIFIED, HOT, NURTURE, BOOKED, plus pending approval).
       - Mock `react-router-dom` (`useNavigate`).
       - Test initial loading state and Kanban board mount (`data-testid="kanban-board"`).
       - Test that all 7 status columns render with proper column headers and counts:
         - `kanban-column-new`
         - `kanban-column-calling`
         - `kanban-column-in_conversation`
         - `kanban-column-qualified`
         - `kanban-column-hot`
         - `kanban-column-nurture`
         - `kanban-column-booked`
       - Test lead cards appear in the exact columns matching their `status`.
       - Test search input filtering:
         - Typing lead name shows only matching cards.
         - Typing area/zipcode filters cards accordingly.
       - Test category pill filtering:
         - Clicking "Hot" filter isolates leads with status HOT or score >= 85.
         - Clicking "In-Flight" isolates CALLING / IN_CONVERSATION.
         - Clicking "Booked" isolates BOOKED leads.
         - Clicking "Approval Needed" isolates leads with `pending_approval: true`.
       - Test KPI card values (Hot count, Booked count, In-Flight count, Conversion Rate calculation).
       - Test "Seed Demo Leads" button calls `api.post("/seed")` and displays success toast.
  </action>
  <verify>npm --prefix frontend test -- --watchAll=false src/pages/Dashboard.test.jsx</verify>
  <acceptance_criteria>
    - All tests in `Dashboard.test.jsx` pass.
    - Kanban columns, filtering, KPI calculations, and seed operations fully verified.
  </acceptance_criteria>
  <done>Kanban board and dashboard verified with automated component tests.</done>
</task>

<task type="auto">
  <name>Task 2: Author Tests for Lead Detail and Capture Intake Pages</name>
  <files>frontend/src/pages/LeadDetail.test.jsx, frontend/src/pages/Capture.test.jsx</files>
  <read_first>frontend/src/pages/LeadDetail.jsx, frontend/src/pages/Capture.jsx</read_first>
  <action>
    1. Create `frontend/src/pages/LeadDetail.test.jsx`:
       - Mock `react-router-dom` `useParams` returning `lead_id: "lead_123"`.
       - Mock `api.get("/leads/lead_123")` and sub-resource calls (`/events`, `/call-logs`, `/appointments`).
       - Test rendering lead details, contact info, and score badge.
       - Test escalation banner appearance when `pending_approval` is true.
       - Test clicking `btn-approve-escalation` calls `POST /leads/lead_123/approve`.
       - Test clicking `btn-reject-escalation` calls `POST /leads/lead_123/reject`.
       - Test clicking `btn-opt-out` calls `POST /leads/lead_123/opt-out`.
    2. Create `frontend/src/pages/Capture.test.jsx`:
       - Render `Capture.jsx` within a memory router.
       - Test filling `input-name`, `input-phone`, `input-email` and submitting `btn-submit-lead`.
       - Test successful `POST /lead` dispatch and navigation/success response.
  </action>
  <verify>npm --prefix frontend test -- --watchAll=false src/pages/</verify>
  <acceptance_criteria>
    - All tests in `LeadDetail.test.jsx` and `Capture.test.jsx` pass.
    - Escalation actions and lead capture flows validated.
  </acceptance_criteria>
  <done>LeadDetail and Capture pages validated with automated tests.</done>
</task>

<task type="auto">
  <name>Task 3: Full Frontend Test Suite Run</name>
  <files>frontend/src/</files>
  <read_first>frontend/package.json</read_first>
  <action>
    1. Run the entire frontend test suite across all unit, component, and page test files.
    2. Ensure 0 failed tests, 0 unhandled promise rejections, and no watch-mode hangs.
  </action>
  <verify>npm --prefix frontend test -- --watchAll=false</verify>
  <acceptance_criteria>
    - 100% test pass rate across all frontend test suites.
    - Requirement UI-01 completely satisfied.
  </acceptance_criteria>
  <done>Frontend testing suite complete and verified end-to-end.</done>
</task>

</tasks>

<must_haves>
  <truths>
    - Kanban board renders 7 status columns and filters leads correctly
    - Dashboard KPIs and lead actions behave deterministically
    - LeadDetail escalation and status actions trigger correct API calls
    - Complete frontend test suite passes 100%
  </truths>
</must_haves>
