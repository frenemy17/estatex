---
phase: 01-real-provider-live-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - backend/providers.py
  - backend/server.py
  - backend/.env.example
  - frontend/src/components/ProviderStatus.jsx
autonomous: true
requirements:
  - PROV-02
  - PROV-03
  - PROV-05

must_haves:
  truths:
    - "Cal.com slot availability query and booking creation execute against live API and return 502 on failure"
    - "Resend email client successfully dispatches nurture emails and booking confirmations"
    - "Twilio SMS dispatches text messages and processes inbound STOP opt-outs"
    - "HubSpot client creates contacts and associates pipeline deals"
    - "GET /api/providers accurately reports LIVE mode for all properly configured services"
  artifacts:
    - backend/providers.py
    - backend/server.py
    - backend/.env.example
    - frontend/src/components/ProviderStatus.jsx
  key_links:
    - "Booking commitments in backend/server.py check live Cal.com results before setting lead status to BOOKED"
    - "Provider health entries are recorded in db.provider_health for all live calls"
---

<objective>
Verify and harden live SaaS integrations for Cal.com (booking), Resend (email), Twilio (SMS), and HubSpot (CRM) with honest status reporting and end-to-end pipeline execution.

Purpose: Ensures that all provider chips on the dashboard reflect genuine operational status, eliminating silent mock fallbacks and confirming that lead progression triggers real-world calendar bookings, CRM updates, and notification delivery.
Output: Hardened provider clients, verified API endpoints, updated environment templates, and accurate frontend status chips.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-real-provider-live-readiness/01-CONTEXT.md
@.planning/phases/01-real-provider-live-readiness/01-RESEARCH.md
@backend/providers.py
@backend/server.py
@frontend/src/components/ProviderStatus.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden Cal.com Booking Provider for Live Slot & Booking Execution</name>
  <files>backend/providers.py, backend/server.py</files>
  <read_first>backend/providers.py, backend/server.py</read_first>
  <action>
    Review and harden `BookingProvider` in `backend/providers.py`:
    1. In `get_slots()`, support both Cal.com v1 (`/v1/slots`) and v2 (`/v2/slots/available`) response formats, ensuring slots are cleanly normalized into ISO timestamp strings.
    2. In `book()`, submit attendee details (`name`, `email`), `start` time, and `eventTypeId` to Cal.com API.
    3. Ensure `server.py` endpoint `POST /api/leads/:id/book` strictly checks `result.ok`. If the live call fails, log the real status/error to `db.provider_health`, return HTTP 502 Bad Gateway with provider details, and ensure the lead does NOT advance to `BOOKED`.
    4. When `is_live("calcom")` is true, verify that the dashboard status chip turns green (`mode: "LIVE", ok: true`).
  </action>
  <verify>backend/.venv/bin/pytest backend/tests/test_providers.py</verify>
  <acceptance_criteria>
    - Cal.com returns normalized slot arrays for both v1 and v2 API payloads.
    - Booking failure raises HTTP 502 and maintains existing lead status.
  </acceptance_criteria>
  <done>Cal.com integration operates live with robust slot normalization and strict failure gating.</done>
</task>

<task type="auto">
  <name>Task 2: Harden Resend Email, Twilio SMS, and HubSpot CRM Live Integrations</name>
  <files>backend/providers.py, backend/server.py</files>
  <read_first>backend/providers.py</read_first>
  <action>
    Harden notification and CRM providers in `backend/providers.py`:
    1. **Resend Email:**
       - Verify payload matches Resend v1 API (`POST https://api.resend.com/emails`).
       - If `DEMO_EMAIL` is configured, redirect recipient address to `DEMO_EMAIL` while preserving buyer name and context in subject/body.
       - Enforce `onboarding@resend.dev` as default sender if custom domain is not yet verified.
    2. **Twilio SMS:**
       - Validate outbound text dispatching via Twilio REST API.
       - Verify that inbound SMS `STOP` webhook sets `opted_out: True` on lead and logs an audit event.
    3. **HubSpot CRM:**
       - Verify contact creation/update via `/crm/v3/objects/contacts`.
       - Implement deal association via `/crm/v3/objects/deals` when lead reaches `HOT` or `BOOKED`.
  </action>
  <verify>backend/.venv/bin/pytest backend/tests/</verify>
  <acceptance_criteria>
    - Resend client properly formats email requests and respects `DEMO_EMAIL` redirection.
    - Twilio SMS webhook idempotently marks opt-out status.
    - HubSpot sync creates contact and deal objects.
  </acceptance_criteria>
  <done>All secondary providers (Resend, Twilio SMS, HubSpot) are verified for live SaaS execution.</done>
</task>

<task type="auto">
  <name>Task 3: Update Frontend ProviderStatus UI & Documentation</name>
  <files>frontend/src/components/ProviderStatus.jsx, backend/.env.example</files>
  <read_first>frontend/src/components/ProviderStatus.jsx, backend/.env.example</read_first>
  <action>
    1. In `frontend/src/components/ProviderStatus.jsx`, verify that provider chips render:
       - Green badge with checkmark when `mode === "LIVE"` and `ok === true`.
       - Yellow badge when `mode === "MOCK"`.
       - Red badge with tooltip containing error details when `mode === "LIVE"` and `ok === false`.
    2. Update `backend/.env.example` with clear instructions on configuring real keys for Groq, Twilio Voice & SMS, Cal.com, Resend, and HubSpot.
  </action>
  <verify>git status -s</verify>
  <acceptance_criteria>
    - `frontend/src/components/ProviderStatus.jsx` accurately reflects live vs mock states.
    - `backend/.env.example` documents all keys and setup steps for zero-paywall live operation.
  </acceptance_criteria>
  <done>Frontend status indicators and configuration documentation are aligned with live SaaS requirements.</done>
</task>

</tasks>

<must_haves>
  <truths>
    - Cal.com bookings execute against live calendar and prevent false bookings on error
    - Resend dispatches live nurture emails with demo redirection safety
    - HubSpot syncs contacts and creates pipeline deals
    - Provider status chips render accurate green/yellow/red indicators
  </truths>
</must_haves>
