---
phase: 03-monolithic-server-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/__init__.py
  - backend/core/database.py
  - backend/core/models.py
  - backend/core/state_machine.py
  - backend/core/rate_limit.py
  - backend/core/scoring.py
  - backend/core/pipeline.py
  - backend/core/tick.py
autonomous: true
requirements:
  - MOD-02

must_haves:
  truths:
    - "backend/core/ package contains database, models, state machine, scoring, pipeline, rate limiting, and tick modules"
    - "get_db() dynamically checks sys.modules['server']._db for fake_db compatibility"
    - "All domain functions (transition, score_and_route, run_ai_pipeline, run_tick) function identically in isolation"
  artifacts:
    - backend/core/database.py
    - backend/core/models.py
    - backend/core/state_machine.py
    - backend/core/rate_limit.py
    - backend/core/scoring.py
    - backend/core/pipeline.py
    - backend/core/tick.py
  key_links:
    - "core.database exports db proxy and to_mongo helper"
    - "core.pipeline orchestrates multi-agent graph"
    - "core.tick drives autonomous background schedule"
---

<objective>
Extract domain logic from `backend/server.py` into dedicated modules inside `backend/core/`.

Purpose: Fulfills requirement `MOD-02` by isolating the state machine, rubric scoring, multi-agent AI pipeline, database proxy, rate limiter, and autonomous scheduler loop from HTTP routing concerns.
Output: Complete, cleanly structured `backend/core/` package ready for consumption by route handlers and `server.py`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-monolithic-server-refactoring/03-CONTEXT.md
@.planning/phases/03-monolithic-server-refactoring/03-RESEARCH.md
@backend/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Database Connection and Pydantic Models</name>
  <files>backend/core/database.py, backend/core/models.py</files>
  <read_first>backend/server.py</read_first>
  <action>
    1. Create `backend/core/database.py`:
       - Define lazy connection globals: `_client`, `_db`.
       - Implement `get_db()` with dynamic check: if `sys.modules.get("server")` has `_db` that is not None, return it (preserving `fake_db` monkeypatching).
       - Implement `close_db()`, `_DBProxy`, and `db = _DBProxy()`.
       - Implement `to_mongo(doc)`, `now()`, and `now_iso()`.
    2. Create `backend/core/models.py`:
       - Define `BaseDoc`, `Lead`, `Event`, `CallLog`, `ScheduledAction`.
       - Define request/response DTOs: `LeadCreate`, `LeadUpdate`, `ManualCallRequest`, `ManualSmsRequest`, `DirectBookingRequest`, `GoogleLeadPayload`, `SimulatePayload`.
  </action>
  <verify>python -c "from backend.core.database import db, now_iso; from backend.core.models import Lead; print(Lead(name='Test', phone='+1234'))"</verify>
  <acceptance_criteria>
    - `core.database` and `core.models` import cleanly without circular references.
    - `Lead` and `ScheduledAction` instantiate with default values.
  </acceptance_criteria>
  <done>Database access and domain models cleanly extracted to backend/core/.</done>
</task>

<task type="auto">
  <name>Task 2: Extract State Machine, Rate Limiting, and Rubric Scoring</name>
  <files>backend/core/state_machine.py, backend/core/rate_limit.py, backend/core/scoring.py</files>
  <read_first>backend/server.py</read_first>
  <action>
    1. Create `backend/core/state_machine.py`:
       - Define `STATUSES`, `ACTIVE`, `TERMINAL`, `ALLOWED_TRANSITIONS`.
       - Implement `can_transition(from_s, to_s) -> bool`.
       - Implement `record_event(...)` and `transition(...)`.
    2. Create `backend/core/rate_limit.py`:
       - Define `_rate_buckets`, `LEAD_RATE_LIMIT_PER_MIN`.
       - Implement `_in_memory_rate_limit(key, limit)`.
       - Implement `async def rate_limit(request, bucket="lead", per_min=None)`.
    3. Create `backend/core/scoring.py`:
       - Define `RUBRIC_WEIGHTS`, `QUALIFICATION_SYSTEM_PROMPT`, `ROUTER_SYSTEM_PROMPT`.
       - Implement `score_and_route(transcript)`.
       - Implement `qualify_and_route(lead_id)`.
  </action>
  <verify>python -c "from backend.core.state_machine import can_transition; assert can_transition('NEW', 'CALLING') is True"</verify>
  <acceptance_criteria>
    - State machine transitions and validations behave identically to existing implementation.
    - Rubric scoring logic operates without dependencies on HTTP request lifecycle.
  </acceptance_criteria>
  <done>FSM, rate limiting, and rubric scoring isolated into backend/core/.</done>
</task>

<task type="auto">
  <name>Task 3: Extract AI Pipeline and Autonomous Tick Engine</name>
  <files>backend/core/pipeline.py, backend/core/tick.py</files>
  <read_first>backend/server.py</read_first>
  <action>
    1. Create `backend/core/pipeline.py`:
       - Define `AgentState`.
       - Implement graph nodes: `agent_node_dialer`, `agent_node_qualifier`, `agent_node_enrichment`, `agent_node_action_routing`.
       - Implement `_compile_pipeline()` and `run_ai_pipeline(lead_id)`.
       - Support graph rebuild when `fake_db` swaps database.
    2. Create `backend/core/tick.py`:
       - Define `MAX_SCHEDULED_ATTEMPTS`, `BACKOFF_BASE_SECONDS`, `MAX_BACKOFF_SECONDS`, `compute_backoff_seconds`.
       - Define interval constants: `CALL_TIMEOUT_MINUTES`, `NURTURE_REQUALIFY_HOURS`, `SUPERVISOR_INTERVAL_HOURS`.
       - Implement `schedule_action(lead_id, kind, run_at, payload, reason)`.
       - Implement `_run_scheduled(doc)`.
       - Implement `run_tick(limit=25)`.
    3. Create `backend/core/__init__.py` exposing primary interfaces.
  </action>
  <verify>python -c "from backend.core.tick import compute_backoff_seconds; assert compute_backoff_seconds(1) == 30"</verify>
  <acceptance_criteria>
    - Multi-agent LangGraph pipeline builds and compiles.
    - Autonomous scheduler with backoff and DLQ logic executes properly.
  </acceptance_criteria>
  <done>Core domain business logic fully modularized under backend/core/.</done>
</task>

</tasks>

<must_haves>
  <truths>
    - All business logic lives in backend/core/
    - get_db() respects test monkeypatching of server._db
    - Multi-agent LangGraph compiles cleanly
    - Autonomous tick engine runs with exponential backoff & DLQ logic
  </truths>
</must_haves>
