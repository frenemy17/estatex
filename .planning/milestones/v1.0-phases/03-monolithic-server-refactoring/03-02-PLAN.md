---
phase: 03-monolithic-server-refactoring
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - backend/routes/__init__.py
  - backend/routes/auth.py
  - backend/routes/leads.py
  - backend/routes/webhooks.py
  - backend/routes/admin.py
  - backend/routes/providers.py
  - backend/server.py
autonomous: true
requirements:
  - MOD-01

must_haves:
  truths:
    - "backend/routes/ package contains leads, webhooks, admin, providers, and auth route controllers"
    - "server.py is reduced to a clean assembly shell mounting the APIRouters"
    - "server.py maintains backward-compatible exports for all domain models, functions, and state variables"
    - "All 78 unit/integration tests in backend/tests/ pass without modifying a single test line"
  artifacts:
    - backend/routes/auth.py
    - backend/routes/leads.py
    - backend/routes/webhooks.py
    - backend/routes/admin.py
    - backend/routes/providers.py
    - backend/server.py
  key_links:
    - "server.py mounts leads, webhooks, admin, and providers routers onto FastAPI app"
    - "All existing endpoint paths (/api/lead, /api/leads/*, /api/webhooks/*, /api/tick, /api/queue/*) respond identically"
---

<objective>
Extract endpoint handlers into `backend/routes/` and assemble the refactored application in `backend/server.py`.

Purpose: Fulfills requirement `MOD-01` by modularizing route controllers into domain-specific files while preserving exact HTTP routing, security dependencies, and full test suite compatibility.
Output: Decoupled `backend/routes/` package, concise `backend/server.py` assembly shell, and 100% green test suite.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-monolithic-server-refactoring/03-CONTEXT.md
@.planning/phases/03-monolithic-server-refactoring/03-RESEARCH.md
@.planning/phases/03-monolithic-server-refactoring/03-01-PLAN.md
@backend/server.py
@backend/tests/test_tick.py
@backend/tests/test_providers.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Authentication and Route Controllers</name>
  <files>backend/routes/auth.py, backend/routes/leads.py, backend/routes/webhooks.py, backend/routes/admin.py, backend/routes/providers.py</files>
  <read_first>backend/server.py</read_first>
  <action>
    1. Create `backend/routes/auth.py`:
       - Define `ADMIN_TOKEN`.
       - Implement `async def require_admin(...)`.
    2. Create `backend/routes/leads.py`:
       - Mount on `/leads` and `/lead`.
       - Implement lead capture: `POST /lead`, `POST /leads/bulk`.
       - Implement lead reads: `GET /leads`, `GET /leads/{id}`, `GET /leads/{id}/events`, `GET /leads/{id}/call-logs`.
       - Implement lead updates & actions: `PATCH /leads/{id}`, `POST /leads/{id}/call`, `POST /leads/{id}/sms`, `POST /leads/{id}/book-appointment`, `POST /leads/{id}/status`, `POST /leads/{id}/rerun`.
    3. Create `backend/routes/webhooks.py`:
       - Mount on `/webhooks` and voice endpoints.
       - Implement `POST /webhooks/google-leads`.
       - Implement `POST /webhooks/vapi`.
       - Implement `POST /voice/twiml` and `POST /voice/gather`.
       - Implement `POST /webhooks/twilio-sms`.
    4. Create `backend/routes/admin.py`:
       - Implement `POST /tick`.
       - Implement `GET /queue/dead-letter` and `POST /queue/dead-letter/{action_id}/retry`.
       - Implement `GET /analytics`.
       - Implement `POST /seed`, `DELETE /reset`, and `POST /simulate`.
    5. Create `backend/routes/providers.py`:
       - Implement `GET /providers` and `POST /providers/{name}/test`.
    6. Create `backend/routes/__init__.py`.
  </action>
  <verify>python -c "from backend.routes import leads, webhooks, admin, providers; print('Routes import clean')"</verify>
  <acceptance_criteria>
    - All routes cleanly import and define APIRouter instances.
    - No circular dependencies across routes and core.
  </acceptance_criteria>
  <done>All route handlers extracted into specialized router modules.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble server.py Shell with Backward-Compatible Exports</name>
  <files>backend/server.py</files>
  <read_first>backend/server.py</read_first>
  <action>
    1. Refactor `backend/server.py`:
       - Define `lifespan` context manager initializing Mongo ping and TTL indexes on `rate_limits`.
       - Instantiate `app = FastAPI(...)`.
       - Configure CORS middleware.
       - Include routers from `backend/routes/` under `/api` prefix:
         - `api.include_router(leads.router)`
         - `api.include_router(webhooks.router)`
         - `api.include_router(admin.router)`
         - `api.include_router(providers.router)`
       - Mount `/api` onto `app`.
       - Define root endpoint `GET /` and health endpoint `GET /health`.
    2. Re-export all symbols from `backend/core/` and `backend/routes/` at `server.py` module level:
       - Module globals: `_client`, `_db`, `_compiled_graph`, `_rate_buckets`, `LEAD_RATE_LIMIT_PER_MIN`, `ADMIN_TOKEN`, `GOOGLE_LEADS_WEBHOOK_KEY`, `VAPI_WEBHOOK_SECRET`.
       - Database: `db`, `get_db`, `close_db`, `to_mongo`, `now`, `now_iso`.
       - Models: `Lead`, `Event`, `CallLog`, `ScheduledAction`, `LeadCreate`, `LeadUpdate`, `ManualCallRequest`, `ManualSmsRequest`, `DirectBookingRequest`, `GoogleLeadPayload`, `SimulatePayload`.
       - FSM & Scoring: `STATUSES`, `ACTIVE`, `TERMINAL`, `ALLOWED_TRANSITIONS`, `can_transition`, `transition`, `record_event`, `score_and_route`, `qualify_and_route`.
       - Pipeline & Scheduler: `run_ai_pipeline`, `schedule_action`, `run_tick`, `compute_backoff_seconds`, `rate_limit`.
       - Route functions called directly in tests: `create_lead`, `list_dead_letter`, `retry_dead_letter`, `voice_twiml_endpoint`, `voice_gather_endpoint`, `google_leads_webhook`, `vapi_webhook`, `inbound_sms_webhook`, `require_admin`.
  </action>
  <verify>backend/.venv/bin/pytest backend/tests/</verify>
  <acceptance_criteria>
    - `server.py` is under 300 lines of code.
    - All 78 tests in `backend/tests/` pass without modification.
  </acceptance_criteria>
  <done>server.py reduced to modular shell while retaining 100% test and runtime compatibility.</done>
</task>

<task type="auto">
  <name>Task 3: Full Test Suite Verification and Uvicorn Smoke Test</name>
  <files>backend/server.py</files>
  <read_first>backend/tests/conftest.py</read_first>
  <action>
    1. Run full pytest suite across `backend/tests/test_tick.py` and `backend/tests/test_providers.py`.
    2. Start uvicorn in a brief smoke check or import test to verify application loads with all routers attached.
  </action>
  <verify>backend/.venv/bin/pytest backend/tests/</verify>
  <acceptance_criteria>
    - 100% test pass rate (78/78 tests).
    - Zero regressions.
  </acceptance_criteria>
  <done>Phase 3 server refactoring verified end-to-end.</done>
</task>

</tasks>

<must_haves>
  <truths>
    - server.py is clean assembly shell mounting modular routers
    - Route handlers reside in backend/routes/
    - All 78 backend tests continue to pass 100%
  </truths>
</must_haves>
