# Runtime
fastapi==0.110.1
uvicorn==0.25.0
motor==3.3.1
pymongo==4.6.3
pydantic>=2.6.4
email-validator>=2.2.0      # pydantic EmailStr on the capture payload
python-multipart>=0.0.9     # form-encoded Twilio inbound-SMS webhook
python-dotenv>=1.0.1
requests>=2.31.0            # provider HTTP calls, run in a worker thread
certifi>=2024.2.2           # TLS CA bundle for mongodb+srv on hosts without one

# Optional LLM fallback: only imported when GROQ_API_KEY holds a non-Groq
# (Google AI Studio) key. Safe to remove — Groq is the primary path and the
# pipeline degrades to deterministic extraction if neither is configured.
google-generativeai

# Tests (offline: no server, no database, no network — see tests/conftest.py)
pytest>=8.0.0
pytest-xdist>=3.6.0
